{
	"global": {
		"chapters.destination": "/data",
		"chapters.format": "${manga} -.- C${chapter} ${title}",
		"chapters.method": "cbz",
		"chapters.separate": true
	},
	"api": {
		"requests.cache.maxlen": 50
	},
	"downloaders": {
		"mangadex.org": {
			"language.filter": "en"
		}
	}
}